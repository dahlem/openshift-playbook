[defaults]
inventory = inventory
roles_path = roles
collections_paths = collections
stdout_callback = default
result_format = yaml
retry_files_enabled = false
# Vault password: create .vault_pass with your password (already in .gitignore)
# Or use --ask-vault-pass on each run
vault_password_file = .vault_pass

[inventory]
enable_plugins = yaml
